TOOL=pinentry

MAJOR=1
MINOR=3
# versioning.sh doesn't support more than 3 version parts
# so to still be able to mark changes to pinentry not related
# to the upstream version, 'misuse' REVISION.
REVISION=1.1

#PRERELEASE=.1
VERSION="${MAJOR}.${MINOR}${REVISION:+.$REVISION}${PRERELEASE}"
